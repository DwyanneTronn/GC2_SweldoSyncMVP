{
  "buildCommand": "chmod +x ./build.sh && ./build.sh",
  "outputDirectory": "frontend_flutter/build/web",
  "installCommand": "echo 'Flutter installed during build'",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

